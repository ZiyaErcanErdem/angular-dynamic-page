import { PredicateOperatorDirective } from './predicate-operator.directive';

describe('PredicateOperatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PredicateOperatorDirective();
    expect(directive).toBeTruthy();
  });
});
