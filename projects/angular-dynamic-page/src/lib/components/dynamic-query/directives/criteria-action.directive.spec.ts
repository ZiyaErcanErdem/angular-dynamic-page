import { CriteriaActionDirective } from './criteria-action.directive';

describe('CriteriaActionDirective', () => {
  it('should create an instance', () => {
    const directive = new CriteriaActionDirective();
    expect(directive).toBeTruthy();
  });
});
