import { CriteriaConditionDirective } from './criteria-condition.directive';

describe('CriteriaConditionDirective', () => {
  it('should create an instance', () => {
    const directive = new CriteriaConditionDirective();
    expect(directive).toBeTruthy();
  });
});
