import { PredicateFieldDirective } from './predicate-field.directive';

describe('PredicateFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new PredicateFieldDirective();
    expect(directive).toBeTruthy();
  });
});
