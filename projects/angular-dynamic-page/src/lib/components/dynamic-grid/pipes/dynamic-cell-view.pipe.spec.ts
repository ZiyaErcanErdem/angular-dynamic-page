import { DynamicCellViewPipe } from './dynamic-cell-view.pipe';

describe('DynamicCellViewPipe', () => {
  it('create an instance', () => {
    const pipe = new DynamicCellViewPipe();
    expect(pipe).toBeTruthy();
  });
});
