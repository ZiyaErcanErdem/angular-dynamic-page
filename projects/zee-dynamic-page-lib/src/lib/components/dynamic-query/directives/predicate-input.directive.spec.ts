import { PredicateInputDirective } from './predicate-input.directive';

describe('PredicateInputDirective', () => {
  it('should create an instance', () => {
    const directive = new PredicateInputDirective();
    expect(directive).toBeTruthy();
  });
});
