import { RemovePredicateDirective } from './remove-predicate.directive';

describe('RemovePredicateDirective', () => {
  it('should create an instance', () => {
    const directive = new RemovePredicateDirective();
    expect(directive).toBeTruthy();
  });
});
