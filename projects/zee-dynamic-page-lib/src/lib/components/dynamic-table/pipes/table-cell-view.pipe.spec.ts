import { TableCellViewPipe } from './table-cell-view.pipe';

describe('TableCellViewPipe', () => {
  it('create an instance', () => {
    const pipe = new TableCellViewPipe();
    expect(pipe).toBeTruthy();
  });
});
